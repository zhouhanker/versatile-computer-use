# VCU 会话交接

更新：2026-09-20。CU-D-170 Settings 只读脚本已加，待 CI。

## 本轮

- settings_like 覆盖 `win:SystemSettings`。
- POC：`ms-settings:` snapshot 后 click/type 必须 FocusPolicyViolation。

## 下一刀

看 CI `SNAP_OK` + `CLICK_DENIED` + `TYPE_DENIED`。未绿不得宣称 170 完成。禁止改系统设置。
