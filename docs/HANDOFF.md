# VCU 会话交接

更新：2026-09-20。CU-D-260 hover 后 bm_click 进行中。

## 本轮

- 260：Guide hover 后 `vcu click` 必须 `bm_click`；按钮副作用发生；`os_cursor_used=false`。无 SendInput。

## 下一刀

看 CI `HOVER_OK` / `INVOKE_OK path=bm_click` / `CU-D-260 OK`。未绿不得宣称 260 完成。不要 claim MAC-NEXT / FEISHU-001。
