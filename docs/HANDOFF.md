# VCU 会话交接

更新：2026-09-20。CU-D-300 CI 已过。下一刀 CU-D-310。

## 本轮

- 300：run `35478128021` @ `6e2fd82` `INVOKE_OK path=bm_click os_cursor_used=False via=mcp_tools_call` / `CU-D-300 OK`。

## 下一刀

CU-D-310：MCP `tools/call` live `vcu_wait` `value` → `scene_wait`。先写进 ROADMAP（已写）再写代码。不要 claim MAC-NEXT / FEISHU-001。
