# VCU 会话交接

更新：2026-09-20。CU-D-290 CI 已过。下一刀 CU-D-300。

## 本轮

- 290：run `35477727787` @ `a6feb7f` `HOVER_OK path=guide_hover os_cursor_used=False via=mcp_tools_call` / `ABORT_OK via=mcp_tools_call` / `CU-D-290 OK`。

## 下一刀

CU-D-300：MCP `tools/call` live `vcu_click` 仍 `bm_click`。先写进 ROADMAP（已写）再写代码。不要 claim MAC-NEXT / FEISHU-001。
