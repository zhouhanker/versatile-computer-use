# VCU 会话交接

更新：2026-09-20。桌面史诗进行中。

## 本轮

- CU-D-023 真机通过并提交 `2b12a1c`。
- CU-D-032 真机通过：`scripts/poc_cu_d_032.py`。ping 0.2.8；现有窗口新标签；screenshot `extension_viewport`；capture dry-run 未按下；throwaway `#counter` `clicks=1` 后关闭；组 1/3 未改。

## 下一刀

CU-D-040 Finder：列目录窗、选图标、回车打开。禁止批量删除。只用脚本自己建的临时文件夹窗口。不要碰 Edge 组 1/3。
