# VCU 会话交接

更新：2026-09-20。CU-D-320 CI 已过。下一刀 CU-D-330。

## 本轮

- 320：run `35479050203` @ `720e6d2` `TYPE_OK path=wm_settext os_cursor_used=False via=mcp_tools_call` / `CU-D-320 OK`。

## 下一刀

CU-D-330：MCP `tools/call` live `vcu_type` 含换行必须拒绝。先写进 ROADMAP（已写）再写代码。不要 claim MAC-NEXT / FEISHU-001。
