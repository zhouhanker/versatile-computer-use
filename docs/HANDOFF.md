# VCU 会话交接

更新：2026-09-20。CU-D-560 已过。

## 本轮

- 560：health `click` 改为 observe 后 `--selector`（不再 pixel-x）；`vcu init` next 改为 observe-bind，不再 tabs-then-screenshot。`scripts/poc_cu_d_560.py` CU-D-560 OK。组 1/3 未动。无 SendInput。不是 TC-B-040。

## 下一刀

停放 P2：TC-B-040 / 跨源 iframe / 产品 Windows CU。不要 claim 完整 Codex CU。不要 claim MAC-NEXT / FEISHU-001。
