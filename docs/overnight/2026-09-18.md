# Overnight log 2026-09-18

Constraint: no UI clicks; no Codex CU; no WeChat; author zhouhanker. Work until 12:00 CST.

## 01:14–01:55

- Implemented `POST /v1/extension/bootstrap` (loopback, no auth).
- Single-instance flock + `already_running`.
- SIGHUP detach (`process_group(0)` + `SIG_IGN`).
- Zero-click Agent Edge: `scripts/start_agent_edge.sh`.
- SW: no `type:module`; offscreen keepalive; persisted `agentWindowId`.
- Default tab resolve prefers agent-owned.
- Extension `scroll`/`wait` via `act`.
- Tests 19 passed; bootstrap + extension session POCs OK.
- Etherscan L1 wordcloud ~655; L2 accounts/tokens; L3 first page ~7 addrs then Sign In wall.
- Feishu: 3 张北北 candidates, 2 exact names — not sent (`~/vcu-feishu-poc/decision.json`).
- Live: `vcu-daemon` pid 13420, `extension_polling=true`.

## Next in this overnight

- Keep daemon/extension alive; more L3 samples; no Allow click.
- If user names which 张北北, send Test via lark-cli.

## 01:55–01:58

- `poc_extension_session` SNAP_OK on example.com.
- CLI `vcu navigate --tab` optional.
- More L3: binance/tether/usdc/okx/coinbase/aave tokens. Public first page then Sign In wall.
- Daemon pid 13420 still polling.

## 01:59

- Research note `docs/research/08-extension-sw-keepalive.md`.
- `vcu navigate --tab` in installed CLI.
- Polling still true ~7 minutes after daemon 13420 start.

## 02:05–02:29

- Verified live daemon, then hardened bootstrap: reject non-extension `Origin` (401), CORS only `chrome-extension`/`moz-extension`/`safari-web-extension`.
- Health now includes `pid` + `last_poll_age_ms`.
- LaunchAgent template `KeepAlive=false` (avoid flock crash-loop). **Not installed** tonight.
- `start_agent_edge.sh`: `open -n -a Microsoft Edge` (direct binary from non-GUI parent dies); pgrep by profile path; health JSON via stdin.
- Extension 0.1.4: wait for tab `complete`, virtual cursor on click/hover, keypress, wait cap 15s, extract NodeList cap 200.
- CLI HTTP timeout 60s; extension command timeout 30s.
- Rebuilt/installed `~/.local/bin/vcu{,-daemon,-mcp}`; daemon pid 20123; user Edge 1168 untouched.
- `poc_extension_bootstrap` OK; evil Origin 401; session example.com SNAP_OK on retry.
- Etherscan extension crawl: L1 **800** `/label/` hrefs; 10 public L3 pages ~11 addrs each (binance accounts one timeout). Full list still behind Sign In. No 9222 Allow click.
- Feishu: IM evidence recorded; **not sent** (2 exact 张北北).
- Watchdog `scripts/overnight_watchdog.sh` keeps daemon + Agent Edge alive until 12:00.
- Tests: `cargo test --workspace` **20 passed**.

## 02:29–02:43

- Agent Edge launcher: `open -n -a Microsoft Edge` (direct exec dies). Watchdog running until 12:00.
- `about:blank` snapshot needed `<all_urls>` host permission.
- poc_extension_session SNAP_OK; hover + virtual cursor DIV present; os_cursor_used=false.
- Etherscan L1 800 hrefs; **18** L3 public pages with address samples. Login wall remains for full lists.
- Feishu still not sent; IM evidence in `~/vcu-feishu-poc/decision.json`.
- Packed `dist/vcu-0.1.0-macos-arm64.tar.gz`.
- MCP `vcu_health` tool added.

## 02:58–03:03

- Tests bind `daemon_port = 0` to avoid `Address already in use`.
- `cargo test --workspace` 20 passed.
- Live: daemon pid 20123, `extension_polling=true`, Agent Edge OK, user Edge 1168 OK, watchdog 29993 until 12:00.
- Codex CU dir still present; WeChat not touched.

## 03:04

- Temporary LaunchAgent `com.vcu.overnight-watchdog` (KeepAlive=false) runs `scripts/overnight_watchdog.sh` until 12:00 CST.
- Not `vcu service install`; daemon flock remains. After noon: `launchctl bootout gui/$(id -u)/com.vcu.overnight-watchdog`.

## 10:40 设计转向

- 用户要求按 Codex CU 结构重设计，名称自有。
- 权威稿：`docs/design/06-stage-steward.md`；调研：`docs/research/10-stage-from-codex-cu-lessons.md`。
- 未实现 overlay/AX 执行器；未动 Codex CU / 微信。
